export const TEXT_COMPONENT = Object.freeze({
    EDITOR: "editor",
    PREVIEWER: "previewer"
});

export const APP_SIZE_STATE = Object.freeze({
    EDITOR: 1,
    PREVIEWER: 2,
    BOTH: 3
})

export const WRAPPER_SIZE_STATE = Object.freeze({
    CLOSED: 0,
    MINIMIZED: 1,
    MAXIMIZED: 2
})

export const COMPONENT_FOCUS = Object.freeze({
    EDITOR: 1,
    PREVIEWER: 2
})